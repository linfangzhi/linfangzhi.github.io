import rss from'@astrojs/rss';import{getCollection}from'astro:content';export async function GET(context){const entries=(await getCollection('content')).sort((a,b)=>b.data.date-a.data.date);return rss({title:'方方编辑部',description:'Podcast、课程、资讯与博客',site:context.site,items:entries.map(e=>({title:e.data.title,pubDate:e.data.date,description:e.data.summary||e.data.subtitle,link:e.data.legacyPath||`/content/${e.id.replace(/\.(md|mdx)$/,'')}/`}))})}
