import {defineCollection,z} from 'astro:content';import {glob} from 'astro/loaders';
const content=defineCollection({loader:glob({pattern:'**/*.{md,mdx}',base:'./src/content/blog'}),schema:z.object({title:z.string(),subtitle:z.string().default(''),date:z.coerce.date(),category:z.string().default('Blog'),type:z.enum(['blog','briefing','course','deep_dive','on_demand','weekly_review']).default('blog'),tags:z.array(z.string()).default([]),summary:z.string().default(''),tldr:z.string().default(''),duration:z.number().optional(),series:z.string().optional(),seriesEpisode:z.number().optional(),audioUrl:z.string().optional(),audioPrivate:z.boolean().default(false),visibility:z.enum(['public','unlisted']).default('public'),legacyPath:z.string().optional(),sources:z.array(z.object({title:z.string(),url:z.string(),publisher:z.string().default(''),publishedAt:z.string().optional()})).default([]),chapters:z.array(z.object({title:z.string(),startSeconds:z.number()})).default([]),keyPoints:z.array(z.string()).default([])})});export const collections={content};
